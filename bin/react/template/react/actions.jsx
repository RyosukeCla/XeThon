const namespace = function(name) {
  return `S_NAME/${name}`;
}

/*
const Actions = {
  hoge: namespace("hoge")
}

export default Actions;

export function hoge(text) {
  return {
    type: Actions.hoge,
    text,
  };
}
*/
